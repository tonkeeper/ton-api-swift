//
// RoundRewardsResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct RoundRewardsResponse: Codable, JSONEncodable, Hashable {

    /** Server-side response time in milliseconds. */
    public var responseTimeMs: Int64
    /** Election ID (electAt timestamp). */
    public var electionId: Int64
    /** Election ID of the round immediately before this one. */
    public var prevElectionId: Int64?
    /** Election ID of the round immediately after this one. */
    public var nextElectionId: Int64?
    /** Validation round start time. */
    public var roundStart: Date
    /** Validation round end time. */
    public var roundEnd: Date
    /** First masterchain block of the round. */
    public var startBlock: Int
    /** Last masterchain block of the round. */
    public var endBlock: Int
    /** amount in nanotons */
    public var totalBonuses: Int64
    /** amount in nanotons */
    public var totalStake: Int64
    public var validators: [ValidatorRewardEntry]
    public var error: String?

    public init(responseTimeMs: Int64, electionId: Int64, prevElectionId: Int64? = nil, nextElectionId: Int64? = nil, roundStart: Date, roundEnd: Date, startBlock: Int, endBlock: Int, totalBonuses: Int64, totalStake: Int64, validators: [ValidatorRewardEntry], error: String? = nil) {
        self.responseTimeMs = responseTimeMs
        self.electionId = electionId
        self.prevElectionId = prevElectionId
        self.nextElectionId = nextElectionId
        self.roundStart = roundStart
        self.roundEnd = roundEnd
        self.startBlock = startBlock
        self.endBlock = endBlock
        self.totalBonuses = totalBonuses
        self.totalStake = totalStake
        self.validators = validators
        self.error = error
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case responseTimeMs = "response_time_ms"
        case electionId = "election_id"
        case prevElectionId = "prev_election_id"
        case nextElectionId = "next_election_id"
        case roundStart = "round_start"
        case roundEnd = "round_end"
        case startBlock = "start_block"
        case endBlock = "end_block"
        case totalBonuses = "total_bonuses"
        case totalStake = "total_stake"
        case validators
        case error
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(responseTimeMs, forKey: .responseTimeMs)
        try container.encode(electionId, forKey: .electionId)
        try container.encodeIfPresent(prevElectionId, forKey: .prevElectionId)
        try container.encodeIfPresent(nextElectionId, forKey: .nextElectionId)
        try container.encode(roundStart, forKey: .roundStart)
        try container.encode(roundEnd, forKey: .roundEnd)
        try container.encode(startBlock, forKey: .startBlock)
        try container.encode(endBlock, forKey: .endBlock)
        try container.encode(totalBonuses, forKey: .totalBonuses)
        try container.encode(totalStake, forKey: .totalStake)
        try container.encode(validators, forKey: .validators)
        try container.encodeIfPresent(error, forKey: .error)
    }
}

