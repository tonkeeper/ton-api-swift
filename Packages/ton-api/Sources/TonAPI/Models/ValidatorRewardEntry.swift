//
// ValidatorRewardEntry.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ValidatorRewardEntry: Codable, JSONEncodable, Hashable {

    public enum PoolType: String, Codable, CaseIterable, CaseIterableDefaultsLast {
        case nominatorPoolV1Period0 = "nominator-pool-v1.0"
        case singleNominatorPoolV1Period0 = "single-nominator-pool-v1.0"
        case singleNominatorPoolV1Period1 = "single-nominator-pool-v1.1"
        case other = "other"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    /** Position sorted by effective stake (descending). */
    public var rank: Int
    /** Validator's public key (hex-encoded Ed25519). */
    public var publicKey: String
    /** amount in nanotons */
    public var effectiveStake: Int64
    /** Fraction of total effective stake (0–1). */
    public var weight: Double
    /** amount in nanotons */
    public var reward: Int64
    /** Pool smart contract address (bounceable, base64url). */
    public var pool: String?
    /** Contract type detected by code hash. */
    public var poolType: PoolType?
    public var ownerAddress: String?
    public var validatorAddress: String?
    /** amount in nanotons */
    public var validatorStake: Int64?
    /** amount in nanotons */
    public var nominatorsStake: Int64?
    /** Total funds deposited by the pool: effective_stake + credit (leftover balance kept in the elector contract after election).  */
    public var totalStake: Int64?
    /** Fraction of staking rewards kept by the validator (0.3 = 30%). */
    public var validatorRewardShare: Double?
    public var nominatorsCount: Int?
    public var nominators: [NominatorRewardEntry]?

    public init(rank: Int, publicKey: String, effectiveStake: Int64, weight: Double, reward: Int64, pool: String? = nil, poolType: PoolType? = nil, ownerAddress: String? = nil, validatorAddress: String? = nil, validatorStake: Int64? = nil, nominatorsStake: Int64? = nil, totalStake: Int64? = nil, validatorRewardShare: Double? = nil, nominatorsCount: Int? = nil, nominators: [NominatorRewardEntry]? = nil) {
        self.rank = rank
        self.publicKey = publicKey
        self.effectiveStake = effectiveStake
        self.weight = weight
        self.reward = reward
        self.pool = pool
        self.poolType = poolType
        self.ownerAddress = ownerAddress
        self.validatorAddress = validatorAddress
        self.validatorStake = validatorStake
        self.nominatorsStake = nominatorsStake
        self.totalStake = totalStake
        self.validatorRewardShare = validatorRewardShare
        self.nominatorsCount = nominatorsCount
        self.nominators = nominators
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case rank
        case publicKey = "public_key"
        case effectiveStake = "effective_stake"
        case weight
        case reward
        case pool
        case poolType = "pool_type"
        case ownerAddress = "owner_address"
        case validatorAddress = "validator_address"
        case validatorStake = "validator_stake"
        case nominatorsStake = "nominators_stake"
        case totalStake = "total_stake"
        case validatorRewardShare = "validator_reward_share"
        case nominatorsCount = "nominators_count"
        case nominators
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(rank, forKey: .rank)
        try container.encode(publicKey, forKey: .publicKey)
        try container.encode(effectiveStake, forKey: .effectiveStake)
        try container.encode(weight, forKey: .weight)
        try container.encode(reward, forKey: .reward)
        try container.encodeIfPresent(pool, forKey: .pool)
        try container.encodeIfPresent(poolType, forKey: .poolType)
        try container.encodeIfPresent(ownerAddress, forKey: .ownerAddress)
        try container.encodeIfPresent(validatorAddress, forKey: .validatorAddress)
        try container.encodeIfPresent(validatorStake, forKey: .validatorStake)
        try container.encodeIfPresent(nominatorsStake, forKey: .nominatorsStake)
        try container.encodeIfPresent(totalStake, forKey: .totalStake)
        try container.encodeIfPresent(validatorRewardShare, forKey: .validatorRewardShare)
        try container.encodeIfPresent(nominatorsCount, forKey: .nominatorsCount)
        try container.encodeIfPresent(nominators, forKey: .nominators)
    }
}

