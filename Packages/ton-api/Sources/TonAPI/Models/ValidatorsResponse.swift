//
// ValidatorsResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct ValidatorsResponse: Codable, JSONEncodable, Hashable {

    /** Server-side response time in milliseconds. */
    public var responseTimeMs: Int64
    public var block: BlockInfo
    public var validationRound: RoundInfo
    /** Current election ID (electAt timestamp). */
    public var electionId: Int64
    /** Election ID of the round immediately before this one. */
    public var prevElectionId: Int64?
    /** Election ID of the round immediately after this one. Omitted when the current round is not yet finished (next round not known).  */
    public var nextElectionId: Int64?
    /** amount in nanotons */
    public var electorBalance: Int64
    /** amount in nanotons */
    public var totalStake: Int64
    /** amount in nanotons */
    public var rewardPerBlock: Int64
    public var validators: [ValidatorRewardEntry]

    public init(responseTimeMs: Int64, block: BlockInfo, validationRound: RoundInfo, electionId: Int64, prevElectionId: Int64? = nil, nextElectionId: Int64? = nil, electorBalance: Int64, totalStake: Int64, rewardPerBlock: Int64, validators: [ValidatorRewardEntry]) {
        self.responseTimeMs = responseTimeMs
        self.block = block
        self.validationRound = validationRound
        self.electionId = electionId
        self.prevElectionId = prevElectionId
        self.nextElectionId = nextElectionId
        self.electorBalance = electorBalance
        self.totalStake = totalStake
        self.rewardPerBlock = rewardPerBlock
        self.validators = validators
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case responseTimeMs = "response_time_ms"
        case block
        case validationRound = "validation_round"
        case electionId = "election_id"
        case prevElectionId = "prev_election_id"
        case nextElectionId = "next_election_id"
        case electorBalance = "elector_balance"
        case totalStake = "total_stake"
        case rewardPerBlock = "reward_per_block"
        case validators
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(responseTimeMs, forKey: .responseTimeMs)
        try container.encode(block, forKey: .block)
        try container.encode(validationRound, forKey: .validationRound)
        try container.encode(electionId, forKey: .electionId)
        try container.encodeIfPresent(prevElectionId, forKey: .prevElectionId)
        try container.encodeIfPresent(nextElectionId, forKey: .nextElectionId)
        try container.encode(electorBalance, forKey: .electorBalance)
        try container.encode(totalStake, forKey: .totalStake)
        try container.encode(rewardPerBlock, forKey: .rewardPerBlock)
        try container.encode(validators, forKey: .validators)
    }
}

