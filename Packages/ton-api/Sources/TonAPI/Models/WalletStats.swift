//
// WalletStats.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct WalletStats: Codable, JSONEncodable, Hashable {

    public var nftsCount: Int
    public var jettonsCount: Int
    public var multisigCount: Int
    public var stakingCount: Int

    public init(nftsCount: Int, jettonsCount: Int, multisigCount: Int, stakingCount: Int) {
        self.nftsCount = nftsCount
        self.jettonsCount = jettonsCount
        self.multisigCount = multisigCount
        self.stakingCount = stakingCount
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case nftsCount = "nfts_count"
        case jettonsCount = "jettons_count"
        case multisigCount = "multisig_count"
        case stakingCount = "staking_count"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(nftsCount, forKey: .nftsCount)
        try container.encode(jettonsCount, forKey: .jettonsCount)
        try container.encode(multisigCount, forKey: .multisigCount)
        try container.encode(stakingCount, forKey: .stakingCount)
    }
}

