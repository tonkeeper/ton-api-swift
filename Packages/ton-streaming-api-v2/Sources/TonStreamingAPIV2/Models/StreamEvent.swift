//
// StreamEvent.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public enum StreamEvent: Codable, JSONEncodable, Hashable {
    case typeAccountStateNotification(AccountStateNotification)
    case typeActionsNotification(ActionsNotification)
    case typeJettonsNotification(JettonsNotification)
    case typeTraceInvalidatedNotification(TraceInvalidatedNotification)
    case typeTraceNotification(TraceNotification)
    case typeTransactionsNotification(TransactionsNotification)

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .typeAccountStateNotification(let value):
            try container.encode(value)
        case .typeActionsNotification(let value):
            try container.encode(value)
        case .typeJettonsNotification(let value):
            try container.encode(value)
        case .typeTraceInvalidatedNotification(let value):
            try container.encode(value)
        case .typeTraceNotification(let value):
            try container.encode(value)
        case .typeTransactionsNotification(let value):
            try container.encode(value)
        }
    }

    private enum DiscriminatorCodingKey: String, CodingKey {
        case type = "type"
    }

    public init(from decoder: Decoder) throws {
        let keyedContainer = try decoder.container(keyedBy: DiscriminatorCodingKey.self)
        let discriminatorValue = try keyedContainer.decode(String.self, forKey: .type)

        switch discriminatorValue {
        case "account_state_change":
            self = .typeAccountStateNotification(try AccountStateNotification(from: decoder))
        case "actions":
            self = .typeActionsNotification(try ActionsNotification(from: decoder))
        case "jettons_change":
            self = .typeJettonsNotification(try JettonsNotification(from: decoder))
        case "trace":
            self = .typeTraceNotification(try TraceNotification(from: decoder))
        case "trace_invalidated":
            self = .typeTraceInvalidatedNotification(try TraceInvalidatedNotification(from: decoder))
        case "transactions":
            self = .typeTransactionsNotification(try TransactionsNotification(from: decoder))
        default:
            throw DecodingError.dataCorrupted(
                DecodingError.Context(
                    codingPath: decoder.codingPath,
                    debugDescription: "Unknown discriminator value '\(discriminatorValue)' for StreamEvent"
                )
            )
        }
    }
}

